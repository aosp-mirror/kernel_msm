LGE RSB rotating side button

Required properties:
- compatible: should be 'rsb'

Example:

&spi_3 {
	rsb@0 {
		compatible = "rsb";
		reg = <0>;
		spi-max-frequency = <2000000>;

		rsb,spi-cs-gpio = <&msm_gpio 2 0x00>;

		pinctrl-names = "default";
		pinctrl-0 = <&rsb_ctrl_active>;
	};
};
